#Ta bort denna sen
from collections import Counter
from dice import final_outcome




def scoreboard():

    scoreboard = {
        "Ones": None, 
        "Twos": None,
        "Threes": None,
        "Fours": None,
        "Fives": None,
        "Sixes": None,
        "One Pair": None,
        "Two Pairs": None,
        "Three of a Kind": None,
        "Four of a Kind": None,
        "Small straight": None,
        "Large straight": None,
        "Full House": None,
        "Chance": None,
        "Yatzy": None,    }
    return scoreboard

def get_options(final_outcome): #This shit is either huge if statement, or I import counter. edit: imported counter
        
        # did some research for counters https://docs.python.org/3/library/collections.html#collections.Counter
        available_categories = []
        user_category = []
        #TODO I mean they're pretty much one rule so simplify into one later
        if scoreboard

#for _ in range(len(score_board)):

score()
calculate_upper_section()
calculate_score(category, dice)
